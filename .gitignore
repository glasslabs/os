buildroot/output/
buildroot/dl/

# Pre-built agent binary (generated by build-agent)
agent/dist/

# RAUC signing key — never commit the private key
buildroot-external/ota/dev-ca.key.pem
buildroot-external/ota/*.key.pem

# Editor / IDE
.idea/
