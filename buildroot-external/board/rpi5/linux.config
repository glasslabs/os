# Raspberry Pi 5 (BCM2712) board-specific kernel config.
# Common settings (systemd, WiFi, DRM, filesystems) live in kernel/common.config.

# RP1 Display Serial Interface — Pi 5 only.
CONFIG_DRM_RP1_DSI=y
