# Raspberry Pi 4 (BCM2711) board-specific kernel config.
# Common settings (systemd, WiFi, DRM, filesystems) live in kernel/common.config.
# No additional options required for rpi4 beyond the shared config.
