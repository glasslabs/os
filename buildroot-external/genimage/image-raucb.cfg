rauc {
    key  = "${GLASSOS_RAUC_KEY}"
    cert = "${GLASSOS_RAUC_CERT}"
    manifest = "${RAUC_MANIFEST}"

    file boot.vfat  { image = "boot.vfat" }
    file kernel.img { image = "kernel.img" }
    file rootfs.img { image = "rootfs.erofs" }
}

